global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: redis-stream-coordinator
    metrics_path: /actuator/prometheus
    static_configs:
      - targets:
          - coordinator:8080
        labels:
          service: coordinator
